//! kan_application_framework_core_configuration_t

${PLUGINS}

root_world {
    world_name = "core"
    scheduler_name = "plain_update"
}

plugin_directory_name = "${PLUGINS_DIRECTORY_NAME}"
resource_directory_name = "${RESOURCES_DIRECTORY_NAME}"
